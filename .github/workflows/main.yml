name: auto_build
on:  workflow_dispatch
jobs: 
  build_project:
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v3
      - name: View dir
        run: ls
        
