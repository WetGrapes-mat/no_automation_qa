#include <iostream>

int main() {
  using namespace std;

  //   cout << "Hello, world!" << endl;

  int a, b;
  cin >> a;
  cin >> b;
  int c = a / b;
  //   cout << c << a / b << endl;

  //   bool is_good = cout.good();

  //   int result = is_good ? EXIT_SUCCESS : EXIT_FAILURE;
  return c;
  // return 0;
}
